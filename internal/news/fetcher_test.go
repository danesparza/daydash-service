package news_test

/*
func TestFetchTweets_DoesNotReturnError(t *testing.T) {
	ctx := context.TODO()

	tweets, err := news.FetchTweets(ctx, "")
	if err != nil {
		t.Errorf("Returned error and we didn't expect that: %v", err)
	}

	//	Spit out what we know:
	for _, tweet := range tweets {
		t.Logf("Tweet (id) text: (%v) %v\n", tweet.ID, tweet.Text)
	}
}

func TestGetStoryAndImageUrl_GetsFullUrlAndImage(t *testing.T) {
	ctx := context.TODO()

	metaInfo, err := news.GetStoryAndImageUrl(ctx, "https://t.co/uHKULXBbFr")
	if err != nil {
		t.Errorf("Returned error and we didn't expect that: %v", err)
	}

	if metaInfo.ImageData == "" {
		t.Errorf("ImageData is empty, and we didn't expect that")
	}

	//	Spit out what we know:
	t.Logf("Story (short) url: %v\n", metaInfo.ShortUrl)
	t.Logf("Story (full) url: %v\n", metaInfo.LongUrl)
	t.Logf("Image url: %v\n", metaInfo.ImageUrl)

}
*/
